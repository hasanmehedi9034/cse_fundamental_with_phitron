#include <stdio.h>
#include <string.h>

int main() {
    // char str[50] = "i am a boy";
    // int address, base;
    // base = (int)(str);
    // address = (int)strchr(str, 'b');

    // printf("%d %d", base, address);

    char tweet[] = "This is my @mention";

    return 0;
}