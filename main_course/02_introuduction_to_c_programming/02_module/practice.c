#include <stdio.h>

int main()
{
    // int marks = 10;
    // scanf("%d", &marks);
    // printf("Marks is % d", marks);

    // return 0;

    int a, b;
	scanf("%d %d", &b, &a);
	a /= b;
	printf("%d %d", a, b);
	return 0;
}