#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);

    for (int cs = 0; cs < t; cs++) {
        int normal_rev, addvert_rev, advert_cost;

        scanf("%d %d %d", &normal_rev, &addvert_rev, &advert_cost);

        int adver_profit = addvert_rev - advert_cost;

        if(adver_profit > normal_rev) {

        }
        else if () {
            
        }
        
    }

    return 0;
}