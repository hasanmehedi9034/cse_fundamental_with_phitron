#include <stdio.h>

int main()

{
	// int a[] = {1,6,3,4,5}, *p = a;

	// p = a;

	// ++*p;

	// printf("%d ", *p);

	// p += 2;

	// printf("%d ", *p);

    // char mystring[] = "abcdefg";

	// char *ptr = mystring;

    // printf("%s",ptr);

    // char mystring[] = "abcdefg";

    // char *ptr = mystring;

    // ptr+=5;

    // printf("%s",ptr);

    char str[]="hello";

    char *ptr = str;

    printf(ptr);

	return 0;
}