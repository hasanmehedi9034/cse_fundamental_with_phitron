#include <stdio.h>

int main() {
    // char ch = 'a';
    // char* pch = &ch;

    // printf("%lld\n", pch);

    // pch++;

    // printf("%lld\n", pch);

    int val1, val2;
    int *pa = &val1;
    int *pb = &val2;

    printf("pa = %lld, pb = %lld", pa, pb);

}